notification:
  email:
    enabled: true
    smtp_server: "smtp.gmail.com"
    smtp_port: 587
    from_email: "aditya@kodekloud.com"
    to_emails:
      - "aditya@kodekloud.com"
#     - "gordon@kodekloud.com"
 #     - "shafaq@kodekloud.com"
  #    - "aakash@kodekloud.com"
   #   - "michael@kodekloud.com"
    #  - "mmumshad@kodekloud.com" #
  
  slack:
    enabled: false
    webhook_url: ""

# Detection settings
detection:
  # Sensitivity for change detection
  # low = only major changes (versions, retirements)
  # medium = moderate changes  
  # high = any content change
  sensitivity: "medium"

# Storage for snapshots
storage:
  type: "json"
  path: "data/cert_snapshots.json"
